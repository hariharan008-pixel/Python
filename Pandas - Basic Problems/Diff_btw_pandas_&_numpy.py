#Usage:
    #In pandas, we use tabular data. | Whereas, in numpy we use numerical data.

#Powerful tools:
    #In pandas, the tools we used is series and data frame. | whereas, in numpy we use arrays.

#Memory:
    #Pandas uses more memory. | whereas, numpy is memory efficient.
    
#Indexing:
    #Indexing is slower in pandas as compared to numpy. | whereas, in numpy insexing is bit faster.